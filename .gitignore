*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/app/release

# Android Studio
*.hprof
.navigation/

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# Other editors
.vscode/

# Signing material -- RIC-201 : the real release keystore lives outside this repo
# (~/cles-signature/, see the signingConfig comment in app/build.gradle.kts) and its path in
# local.properties is already gitignored above. These patterns are defense in depth, in case a
# keystore or its password ever end up copied into the repo by mistake.
*.jks
*.keystore
*.p12
key.properties

# Internal planning docs — working base, not meant to be published
/docs/

# Local tooling — not meant to be published
/.claude/
/.kotlin/

# Personal GPX tracks used as real-world test data — not meant to be published
/traces réelles/

# RIC-187 : cache de tools/i18n/generate_strings.py (script Python, premiere introduction de
# Python dans le projet)
__pycache__/
*.pyc
