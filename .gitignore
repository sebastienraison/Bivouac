*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/app/release

# Android Studio
*.hprof
.navigation/

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# Other editors
.vscode/

# Signing material — no release keystore exists yet (F-Droid builds and signs from source,
# see the signingConfig comment in app/build.gradle.kts), excluded ahead of time in case a real
# prod keystore is introduced later.
*.jks
*.keystore
key.properties

# Internal planning docs — working base, not meant to be published
/docs/

# Local tooling — not meant to be published
/.claude/
/.kotlin/

# Personal GPX tracks used as real-world test data — not meant to be published
/traces réelles/
